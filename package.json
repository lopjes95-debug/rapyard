{
  "name": "rapyard",
  "private": true,
  "packageManager": "pnpm@9.1.0",

  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },

  "devDependencies": {
    "turbo": "2.9.12",
    "typescript": "^5.4.0"
  },

  "engines": {
    "node": ">=18.18.0",
    "pnpm": ">=9.0.0"
  }
}
